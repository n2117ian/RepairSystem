# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RepairSystem_2' do
  # Comment the next line if you don't want to use dynamic frameworks
  
  # Pods for RepairSystem_2
  
  pod 'Firebase/Auth'
  pod 'Firebase/Analytics'
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'mailcore2-ios'
  target 'RepairSystem_2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RepairSystem_2UITests' do
    # Pods for testing
  end

end
